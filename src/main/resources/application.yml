server:
  port: 8080
  servlet:
    context-path: /

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog
    username: blog_user
    password: blog_user
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: update